---
interface Props {
  caption?: string;
  tone?: 'canvas-alt' | 'ink';
  class?: string;
}
const { caption = 'Fotografia', tone = 'canvas-alt', class: className = '' } = Astro.props;
---
<div class:list={['tc-image-slot', tone === 'ink' && 'tc-image-slot--ink', className]}>
  {caption}
</div>

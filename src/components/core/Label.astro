---
interface Props {
  as?: 'div' | 'span' | 'h3';
  class?: string;
}
const { as: Tag = 'div', class: className = '' } = Astro.props;
---
<Tag class:list={['tc-label', className]}>
  <slot />
</Tag>

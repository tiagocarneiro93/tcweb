---
interface Props {
  as?: 'div' | 'span' | 'h3';
  pill?: boolean;
  class?: string;
}
const { as: Tag = 'div', pill = false, class: className = '' } = Astro.props;
---
<Tag class:list={['tc-label', pill && 'tc-eyebrow', className]}>
  <slot />
</Tag>

---
interface Props {
  href: string;
  active?: boolean;
}
const { href, active = false } = Astro.props;
---
<a href={href} class="tc-nav-link" aria-current={active ? 'page' : undefined}>
  <slot />
</a>

---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---
<div>
  <div class="tc-stat__value">{value}</div>
  <div class="tc-stat__label">{label}</div>
</div>
